可以在一条语句中声明多个同类型的变量
在变量声明语句中对变量进行赋值叫变量的初始化，
       所有变量都应该初始化

变量可以用来代表一个固定的未知数字

变量也可以用来代表一组相关数字（任何时候只使用其中
      一个数字，每个数字使用完成后就不再使用了）

计算机内存中的每个存储位置有一个编号，不同存储位置
       的编号不同
只有通过编号才能找到存储位置
这个编号叫做地址
计算机可以根据一个变量找到一个地址，根据这个地址
       才能找到一个存储位置
当不能通过变量找到对应存储位置的时候就需要直接
       通过地址来找

标准函数scanf可以在程序运行时把用户在键盘上输入的
       数字记录在存储位置中
为了使用这个标准函数需要包含stdio.h文件
scanf函数调用语句中必须使用地址表示存储位置
应该使用占位符表示存储位置的类型
scanf函数调用语句中不要加\n
双引号中可以加入不是占位符的内容，但用户必须把
      这些不是占位符的内容在键盘上按顺序输入，否则
      计算机不能得到正确的数据
可以在一条函数调用语句中获得多个数据（占位符的
     个数必须和存储位置的个数一致并且类型也必须
     一致）
每次从键盘读取数据后应该把可能留在输入缓冲区
     里的垃圾数据丢掉

字符类型是一种数据类型，用char表示
这个数据类型中一共包含256个不同的数值，其中
       每个数值可以用来代表一个字符（例如'a',
       '+','%','5'....）
每个字符数值有两个身份：字符身份和数字身份
ASCII码表记录所有字符数值两个身份间的对应关系
'a'        --                97
'A'        --               65
'0'        --               48
256个字符被分成两组，每组128个
其中一组的数字身份在所有计算机上都一样，它们的
       数字身份从0到127
另外一组的数字身份在不同的计算机上可能不同，有
      可能从-128到-1也可能是128到255

所有小写英文字符的数字身份是连续排列的，‘a’的
      数字身份最小，'z'的数字身份最大
所有大写英文字符的数字身份也是连续排列的，'A'的
      数字身份最小，'Z'的数字身份最大
所有阿拉伯数字字符的数字身份也是连续排列的，'0'
     的数字身份最小，字符'9'的数字身份最大

'd' - 'a'   等于   'D' - 'A'
'd' - 'a'   等于   '3' - '0'

无符号字符类型也是一种数据类型，用unsigned char
       表示
这个数据类型同样包含256个字符数值
这个数据类型规定所有字符数值的数字身份是从0到255

'\n'               换行字符
'\r'                回车字符（把打印位置调整到行开头）
'\t'                制表符（让后边的字符出现在下一段的开头）
'\\'                表示\
'\"'                表示"
'\''                 表示'

当希望在程序中处理文字信息的时候可以考虑采用字符类型

短整数是一种数据类型，其中包含65536个不同的整数，
      范围从-32768到32767
短整数使用short表示
无符号短整数使用unsigned short表示
无符号短整数同样包含65536个不同的整数，范围从
       0到65535

长整数也是一种数据类型，用long或long int表示
在我们的计算机上长整数包含2的32次方个不同的整数，
       范围从-2的31次方到2的31次方减一
无符号长整数用unsigned long或unsigned long int表示
整数范围从0到2的32次方减一为止

整数类型用int表示
在我们的计算机上和长整数一样

无符号整数类型用unsigned int表示
在我们的计算机上和无符号长整数一样

整数类型之间的区别主要是所表示的数字范围不同，它们所
      表示的数字范围是逐渐扩大的

C语言中使用浮点类型表示带小数点的数字
浮点类型分为单精度浮点类型和双精度浮点类型
单精度浮点类型用float表示，双精度浮点类型用double表示
单精度浮点类型能记录的小数点后面的数位少，双精度
      浮点类型记录的小数点后面的数位多

程序中整数后面可以加上u表示这个整数是无符号整数类型的

程序中带小数点的数字默认是double类型的，可以在数字
       后面加f表示它是单精度浮点类型的

数据类型和占位符的对应关系
char                      --                          %c和%hhd
unsigned char     --                          %c和%hhu
              %c对应字符身份，%hhd和%hhu对应数字身份
short                     --                          %hd
unsigned short    --                          %hu
long                      --                           %ld
unsigned long      --                          %lu
int                          --                          %d
unsigned int         --                          %u
float                       --                          %f或%g
double                   --                          %lf或%lg
        %f和%lf会保留小数点后面无效的0而%g和%lg不会

预习：
        1.内存结构
        2.数字类型转换
        3.sizeof关键字
        4.二进制

作业：
        1.在屏幕上打印如下内容
            54321
            5432
            543
            54
            5
             要求和上课练习一样
         2.编写程序计算用户给定数字的相反数
         3.编写程序计算给定矩形的面积（矩形的长和
                  宽由用户决定）
         4.编写程序计算给定摄氏温度对应的华氏温度并
                  把结果打印在屏幕上
                  华氏温度   =   1.8 X 摄氏温度 + 32





